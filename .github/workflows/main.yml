name: build
run-name: Build demo pages
on:
  push:
    branches: 
    - main

# jobs:
#   build-explore:
#     uses: ./.github/workflows/explore.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: do nothing
        run: |
          echo "done"
