name: build
run-name: Build demo pages
on:
  push:
    branches: 
    - main

jobs:
  checkout:
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.token }}

  build-explore:
    uses: ./.github/workflows/explore.yml
